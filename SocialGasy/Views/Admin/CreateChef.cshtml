@model SocialGasy.Models.User

@{
    ViewData["Title"] = "Création de Chef Régional";
}

<div class="page-background-alt py-5">
    <div class="container animate__animated animate__fadeIn">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="card shadow-lg border-0 rounded-5 overflow-hidden">
                    <div class="card-header bg-primary text-white p-4 text-center border-0">
                        <h4 class="mb-0 fw-bold"><i class="bi bi-person-plus-fill me-2"></i> Ajouter un Nouveau Chef</h4>
                    </div>
                    
                    <div class="card-body p-4 p-md-5 bg-white">
                        <form asp-action="CreateChef" asp-controller="Admin" method="post" autocomplete="off">
                            @Html.AntiForgeryToken()
                            <div asp-validation-summary="ModelOnly" class="text-danger mb-4"></div>
                            <input type="hidden" asp-for="Role" value="ChefRegional" />

                            <div class="row g-3">
                                <div class="col-md-6 mb-3">
                                    <label asp-for="FullName" class="form-label text-dark fw-bold small">Nom complet</label>
                                    <input asp-for="FullName" class="form-control form-control-lg rounded-4 shadow-sm border-secondary border-opacity-25" placeholder="Ex: Rakoto Be" required />
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label asp-for="Username" class="form-label text-dark fw-bold small">Nom d'utilisateur</label>
                                    <input asp-for="Username" class="form-control form-control-lg rounded-4 shadow-sm border-secondary border-opacity-25" placeholder="Ex: rakoto123" required />
                                </div>
                            </div>

                            <div class="row g-3">
                                <div class="col-md-6 mb-3">
                                    <label asp-for="Cin" class="form-label text-dark fw-bold small">Numéro CIN</label>
                                    <input asp-for="Cin" class="form-control form-control-lg rounded-4 shadow-sm border-secondary border-opacity-25" placeholder="123456789012" required />
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label asp-for="PhoneNumber" class="form-label text-dark fw-bold small">Téléphone</label>
                                    <input asp-for="PhoneNumber" class="form-control form-control-lg rounded-4 shadow-sm border-secondary border-opacity-25" placeholder="034 00 000 00" required />
                                </div>
                            </div>

                            <div class="mb-3">
                                <label asp-for="Address" class="form-label text-dark fw-bold small">Adresse</label>
                                <input asp-for="Address" class="form-control form-control-lg rounded-4 shadow-sm border-secondary border-opacity-25" placeholder="Ex: Antananarivo" required />
                            </div>

                            <div class="row g-3">
                                <div class="col-md-6 mb-3">
                                    <label asp-for="Email" class="form-label text-dark fw-bold small">Adresse Email</label>
                                    <input asp-for="Email" type="email" class="form-control form-control-lg rounded-4 shadow-sm border-secondary border-opacity-25" placeholder="chef@email.com" required />
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label asp-for="Password" class="form-label text-dark fw-bold small">Mot de passe</label>
                                    <input asp-for="Password" type="password" class="form-control form-control-lg rounded-4 shadow-sm border-secondary border-opacity-25" required />
                                </div>
                            </div>

                            <div class="d-grid gap-2 d-md-flex justify-content-md-end mt-4">
                                <a asp-action="ListChefs" class="btn btn-outline-secondary btn-lg rounded-4 py-3 px-4 border-0 hover-lift">Retour</a>
                                <button type="submit" class="btn btn-primary btn-lg rounded-4 py-3 px-5 shadow-sm hover-lift fw-bold">
                                    <i class="bi bi-save me-2"></i> Enregistrer le Chef
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .page-background-alt {
        min-height: 100vh;
        background: linear-gradient(135deg, #0f172a 0%, #334155 100%);
    }
    .hover-lift { transition: transform 0.3s ease; }
    .hover-lift:hover { transform: translateY(-3px); }
    .rounded-5 { border-radius: 2rem !important; }
    .rounded-4 { border-radius: 1.25rem !important; }
    
    @@keyframes fadeIn {
        from { opacity: 0; transform: translateY(20px); }
        to { opacity: 1; transform: translateY(0); }
    }
    .animate__fadeIn { animation: fadeIn 0.8s ease-out forwards; }
</style>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}